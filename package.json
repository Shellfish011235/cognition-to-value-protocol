{
  "name": "cognition-to-value-protocol",
  "version": "1.0.0",
  "description": "Modular, Auditable, Ledger-Native Intelligence Architecture implementing OODA → LEAR → FEYNMAN → PIE → CAR protocol",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:defensive-ooda": "tsc -p tsconfig.defensive-ooda.json",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "test:defensive-ooda": "npm run build:defensive-ooda && node --test dist-defensive-ooda/tests/*.test.js",
    "clean": "rm -rf dist dist-defensive-ooda"
  },
  "keywords": [
    "xrpl",
    "ilp",
    "cognition",
    "ai-safety",
    "payment-intent",
    "ledger",
    "protocol"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "xrpl": "^3.0.0",
    "uuid": "^9.0.0",
    "zod": "^3.22.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
